node_modules/
.env
google-key.json
kluch.env
npm-debug.log
yarn-debug.log
yarn-error.log
.DS_Store
*.log
sheets.js
tokens.txt
ai-server/.env
.env.local
.next/

# B6 — Python venv and bytecode
ai-server/venv/
ai-server/__pycache__/
**/__pycache__/
*.pyc

# B6 — dev SQLite database
ai-server/b6_dev.db
ai-server/b6_dev.db-journal
ai-server/*.sqlite
ai-server/*.sqlite3

# B6 — backup file from refactor
ai-server/main.py.backup

# B6 — Claude Code session state (per-machine, never commit)
.claude/

# B6 — runtime data with real user info (never commit production data)
ai-server/waitlist.db
ai-server/sent_emails.jsonl
ai-server/alembic.ini.bak

# B6 — production environment file (NEVER commit)
.env.prod
.env.production
ai-server/.env.prod

# B6 — legacy backup of old Kampaio landing
src/app/page.kampaio-legacy.tsx.bak

# B6 — frontend build artifacts (already in .next/)
out/
build/

# TypeScript build cache
tsconfig.tsbuildinfo
