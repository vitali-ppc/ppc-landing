KAMPIO AI Assistant - Current Status (оновлено 2025-08-09)
🎯 Основна мета проекту
Створення AI-асистента для аналізу та оптимізації Google Ads кампаній з інтеграцією реальних даних через Google Ads API.

✅ Реалізовані функції
1. AI Chat з Google Ads інтеграцією
✅ Інтеграція з Google Ads API v20

✅ OAuth2 автентифікація з refresh tokens

✅ Отримання реальних даних кампаній

✅ Передача даних в AI для аналізу

✅ Кешування відповідей

✅ Експорт результатів (TXT, CSV, XLSX, JSON, PDF)

2. Технічна архітектура
✅ FastAPI backend

✅ Next.js frontend

✅ Docker контейнеризація

✅ Розгортання на Hetzner сервері

✅ HTTPS з Let's Encrypt

3. Google Ads API інтеграція
✅ Підтримка MCC (Manager) акаунтів

✅ Отримання списку доступних акаунтів

✅ Запит даних кампаній за останні 30 днів

✅ Обробка метрик (кліки, покази, конверсії, витрати)

✅ Правильна обробка структури даних API v20

🔧 Поточні проблеми та статус (оновлено)
Статус: 🟢 Сервер працює стабільно — критичних блокерів немає

Що виправлено останнім часом:
- ✅ Вилучено IndentationError у `ai-server/main.py` (невірні відступи) і перескладено образ без кешу
- ✅ Уніфіковано обробку Google Ads: `/ads-data-real` тепер викликає `get_real_ads_data_internal(...)`, одна точка парсингу
- ✅ Нормалізовано ідентифікатори: `GOOGLE_ADS_CUSTOMER_ID` і `GOOGLE_ADS_CLIENT_CUSTOMER_ID` — без дефісів, один і той же OAuth‑клієнт на фронті та бекенді
- ✅ Додано надійне приведення типів метрик (clicks, impressions, conversions, costMicros, averageCpc) — усунуто `TypeError` при порівняннях
- ✅ `/ads-data-real` повертає 200 OK; запити `googleAds:searchStream` — 200; `/chat` — 200; у логах: “Ads summary present: yes”

Поточний стан:
- AI відповідає з урахуванням реальних метрик; у відповідях формуються таблиці з кампаніями
- Витрати можуть бути $0 у відповідях, якщо за період витрати 0 або поля відсутні в даних — це очікувано для паузних кампаній

Рекомендовані покращення (не блокери):
- 🔸 Обмежувати контекст до топ‑N кампаній (напр., 10) для пришвидшення відповідей
- 🔸 Кешувати Ads‑дані на 10–15 хв (зменшить навантаження і латентність)
- 🔸 Додати у фронті таблицю топ‑кампаній над відповіддю AI

📋 Виявлені деталі (ОНОВЛЕНО 2025-01-06)
✅ Google Ads API працює коректно — отримуються реальні дані 47 кампаній

✅ Структура даних правильна — містить реальні назви кампаній та метрики

✅ Активні кампанії є — 2 кампанії з реальною активністю за останні 30 днів

✅ Більшість кампаній зупинені (PAUSED) — це нормально, активні тільки 2

✅ API route /api/chat правильно передає дані в AI сервер

✅ JSON парсинг виправлено — більше немає помилки Invalid \escape

✅ Дублі функцій видалено — залишилася тільки одна функція /chat

✅ Додано детальне логування з request_id та breadcrumbs

❌ КРИТИЧНА ПРОБЛЕМА: IndentationError блокує запуск сервера
   - Помилка: `IndentationError: unindent does not match any outer indentation level`
   - Місце: `/app/main.py`, строка 763
   - Результат: Docker контейнер в стані `Restarting`
   - Наслідок: Сервер не може запуститися, AI аналіз не виконується
   
   **ПРИКЛАД ПРОБЛЕМНОГО КОДУ НА СЕРВЕРІ**: 
   ```python
   try:
       # Отримуємо валідний access token
           valid_access_token = await get_valid_access_token(access_token, refresh_token)  # ЛИШНІЙ ВІДСТУП
       if not valid_access_token:
           return {"error": "Failed to validate access token"}
   ```
   Правильно має бути:
   ```python
   try:
       # Отримуємо валідний access token
       valid_access_token = await get_valid_access_token(access_token, refresh_token)  # ПРАВИЛЬНИЙ ВІДСТУП
       if not valid_access_token:
           return {"error": "Failed to validate access token"}
   ```

🔍 Діагностика завершена:
- ✅ Google Ads API v20 — працює
- ✅ Дані кампаній — отримуються, парсинг стабільний
- ✅ `/ads-data-real` і `/chat` — 200 OK
- ✅ JSON парсинг і логування — в нормі

🚀 Наступні кроки
1) UX/Продукт
   - Обмежити контекст до топ‑10 кампаній у промпті
   - Додати кэш Ads‑даних (10–15 хв)
   - Додати у фронті таблицю з метриками (топ‑кампанії) перед відповіддю

2) Надійність
   - Ретраї/таймаути для запитів у Google Ads (429/500)
   - Явні повідомлення при `invalid_grant`/401/403

3) Тести
   - Юніт‑тести на парсинг поширених форматів метрик (рядки/числа, `costMicros/averageCpc`)

📊 Поточні метрики (ОНОВЛЕНО)
Google Ads дані (акаунт 702-476-4145):
Всього кампаній: 47

Активних кампаній: 2 (ENABLED)

Зупинених кампаній: 45 (PAUSED)

Загальні витрати: $8.91

Загальні кліки: 436

Загальні покази: 11,056

Загальні конверсії: 15

Активні кампанії (з реальними даними):
✅ Search_Lysechko_EU — 371 клік, 14 конверсій, $7.36
✅ Search_Lysechko_Vit_Ru — 65 кліків, 1 конверсія, $1.55

🔧 Технічні деталі
API Endpoints:
POST /chat — основний chat endpoint (працює, підмішує Ads‑контекст)

POST /ads-data-real — отримання Google Ads даних (200 OK)

GET /ads-data — тестові дані

Змінні середовища (приклад):
GOOGLE_ADS_CUSTOMER_ID=8524763350      # MCC без дефісів
GOOGLE_ADS_CLIENT_CUSTOMER_ID=7024764145  # Client без дефісів
GOOGLE_ADS_DEVELOPER_TOKEN=...
GOOGLE_CLIENT_ID=...  
GOOGLE_CLIENT_SECRET=...

Примітка: фронт і бек мають використовувати один і той самий OAuth‑клієнт. `login-customer-id` має бути саме MCC (manager=true).

Структура даних Google Ads API v20:
```json
[{
  "results": [
    {
      "campaign": {
        "name": "Search_Lysechko_EU",
        "status": "ENABLED"
      },
      "metrics": {
        "clicks": "371",
        "conversions": 14,
        "costMicros": "7359829617"
      }
    }
  ]
}]
```

Troubleshooting (швидкі підказки):
1) 401/`invalid_grant` — протух/інший OAuth‑клієнт: перелогін у фронті (access_type=offline, prompt=consent), однаковий CLIENT_ID/SECRET на фронті й беку
2) 403 "login customer id not a valid manager" — `GOOGLE_ADS_CUSTOMER_ID` має бути MCC (manager=true)
3) 500 при 200 від Google — перевірити приведення типів метрик і структуру searchStream (чанки/результати)

## Постмортем: чому проблеми затягнулися

- Старий код у контейнері: `IndentationError` через мікс табів/пробілів у `main.py`, а Docker кешував криву версію → сервер не стартував.
- `.env` був зламаний: ID з дефісами, зайві символи, склеєні рядки → контейнер не бачив потрібні змінні.
- Токени: підставлялись плейсхолдери/токени іншого OAuth‑клієнта → `invalid_grant` і навіть Unicode‑помилки в заголовках.
- Логіка чату: Ads‑контекст спочатку не підмішувався у промпт належним чином → відповіді «без даних».
- Парсинг метрик: значення приходили рядками (`clicks`, `impressions`, `costMicros`, `averageCpc`) → `TypeError` на порівняннях; виправлено явним приведенням типів.
- Розсинхрон коду: у контейнері крутилася стара гілка («OLD CHAT») → потрібна пересбірка з `--no-cache`.
- Супутні фактори: шум у логах, кеш фронту, кілька паралельних симптомів — ускладнили діагностику.

📝 Останні зміни (ОНОВЛЕНО 2025-01-06)
2025-01-06 (СЬОГОДНІ - КРИТИЧНА ЗНАХІДКА):
✅ Виправлено JSON парсинг — більше немає помилки Invalid \escape

✅ Видалено дублі функцій /chat — залишилася тільки одна функція

✅ Додано детальне логування з request_id та breadcrumbs

✅ Додано "страховочний" try-except блок для ранньої діагностики

❌ КРИТИЧНА ПРОБЛЕМА ЗНАЙДЕНА: IndentationError на строкі 763
   - Помилка: `IndentationError: unindent does not match any outer indentation level`
   - Результат: Docker контейнер в стані `Restarting`
   - Наслідок: Сервер не може запуститися

✅ ДОДАНО: Перевірка синтаксису локально (`python -m py_compile main.py`)

✅ ДОДАНО: Перевірка коду на сервері (`sed -n '760,770p' main.py`)

✅ ДОДАНО: Пересборка Docker без кешу (`docker-compose build --no-cache`)

🔄 ПОТРЕБУЄ ВИПРАВЛЕННЯ: IndentationError все ще блокує запуск сервера

2025-01-06 (раніше):
✅ Виправлено обробку структури даних Google Ads API v20

✅ Додано діагностичні логи

✅ Підтверджено отримання 47 кампаній з реальними назвами

✅ Виявлено та виправлено проблему з передачею adsData з фронтенду

✅ Додано передачу accessToken та refreshToken

✅ Оновлено API route для передачі даних в AI сервер

2025-01-05:
✅ Оновлено API з v14 до v20

✅ Виправлено помилку DEVELOPER_TOKEN_INVALID

✅ Налаштовано правильні customer_id та login_customer_id

Поточний статус проекту:
🔴 БЛОКУЮЧА ПОМИЛКА — Виправлено JSON парсинг та дублі функцій, додано детальне логування, але сервер не може запуститися через IndentationError на строкі 763. Локально синтаксис файлу коректний, проблема в деплої. Потрібно пересобрати Docker образ без кешу та переконатися, що на сервер потрапляє правильна версія файлу.

